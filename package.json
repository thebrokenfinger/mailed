{
  "name": "mailed",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "8.0.1",
    "microbundle": "^0.15.1",
    "prettier": "^3.1.1",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "publish": "lerna publish"
  }
}
